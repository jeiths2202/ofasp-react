       PGM
       DEFLIBL OBJL(TESTLIB) FILEL(TESTLIB)

       OVRF FILE(EMP-FILE) TOFILE(EMPLOYEE.FB.TESTLIB) TYPE(*DATA)

       OVRDSPF FILE(DISPFILE) TOFILE(MAIN001.SMED) DEV(WS0001)

       CALL PGM-MAIN001.JAVA,VOL-DISK01

       DLTOVR FILE(EMP-FILE)
       DLTOVR FILE(DISPFILE)

       ENDPGM
